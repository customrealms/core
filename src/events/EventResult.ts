export enum EventResult {
  ALLOW = Java.resolve('org.bukkit.event.Event.Result').ALLOW,
  DEFAULT = Java.resolve('org.bukkit.event.Event.Result').DEFAULT,
  DENY = Java.resolve('org.bukkit.event.Event.Result').DENY,
}
